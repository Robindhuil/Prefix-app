generator client {
  provider = "prisma-client-js"
  output   = "../app/generated/prisma/client"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id             Int      @id @default(autoincrement())
  username       String   @unique
  email          String?  @unique
  name           String?
  hashedpassword String
  role           Role     @default(USER)
  isActive       Boolean  @default(true)
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  // Dokumenty patriace používateľovi
  documents Document[] @relation("UserDocuments")

  // Dokumenty, ktoré nahral
  uploaded Document[] @relation("DocumentCreator")

  // 🔹 Priradenia do pracovných období
  assignments UserAssignment[]

  // Password reset tokens
  resetTokens PasswordResetToken[]
}

enum Role {
  ADMIN
  USER
}

model CompanyInfo {
  companyId             String   @id
  companyName           String
  companyIdNumber       String
  legalForm             String
  foundingDate          DateTime
  registeredOffice      String
  shareCapital          String
  representation        String
  representativeName    String
  representativeAddress String
  functionStartDate     DateTime
  contactEmail          String
  createdAt             DateTime @default(now())
  updatedAt             DateTime @updatedAt
}

// ========================================
// DOKUMENTY (pre MEGA / GCS / S3)
// ========================================
model Document {
  id        Int  @id @default(autoincrement())
  userId    Int
  user      User @relation("UserDocuments", fields: [userId], references: [id], onDelete: Cascade)
  createdBy Int
  creator   User @relation("DocumentCreator", fields: [createdBy], references: [id])

  fileName String
  mimeType String
  size     Int?
  data     Bytes // ← tu sa uloží obsah súboru

  iv         String?
  tag        String?
  keyVersion Int?
  hash       String?

  documentType DocumentType
  description  String?

  createdAt       DateTime             @default(now())
  updatedAt       DateTime             @updatedAt
  deletedAt       DateTime?
  assignmentLinks AssignmentDocument[]

  @@index([userId])
  @@index([createdBy])
  @@index([documentType])
  @@index([createdAt])
}

enum DocumentType {
  INVOICE
  ORDER
  CONTRACT
  OTHER
}

// ========================================
// PRACOVNÉ OBDOBIA / PROJEKTY / KARTY
// ========================================
model WorkPeriod {
  id          Int      @id @default(autoincrement())
  title       String // napr. "Stavba A – marec 2025"
  startDate   DateTime
  endDate     DateTime
  description String?

  // Požiadavka – koľko ľudí a akých profesií je treba
  requirements WorkRequirement[]

  // Priradení pracovníci
  assignments UserAssignment[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

// ========================================
// POŽIADAVKY NA PROFESIE V DANOM OBDOBÍ
// ========================================
model WorkRequirement {
  id           Int        @id @default(autoincrement())
  profession   Profession
  countNeeded  Int
  workPeriod   WorkPeriod @relation(fields: [workPeriodId], references: [id], onDelete: Cascade)
  workPeriodId Int
}

enum Profession {
  WELDER
  BRICKLAYER
  OTHER
}

// ========================================
// PRIRADENIE UŽÍVATEĽA K OBDOBIU
// ========================================
model UserAssignment {
  id           Int @id @default(autoincrement())
  userId       Int
  workPeriodId Int

  // flexibilné obdobie
  fromDate DateTime
  toDate   DateTime

  // 👇 pridané pole s default hodnotou
  profession Profession @default(OTHER)

  user       User       @relation(fields: [userId], references: [id], onDelete: Cascade)
  workPeriod WorkPeriod @relation(fields: [workPeriodId], references: [id], onDelete: Cascade)

  // Dokumenty špecifické pre daného užívateľa v tomto období
  documents AssignmentDocument[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([userId])
  @@index([workPeriodId])
}

// ========================================
// DOKUMENTY NAVIAZANÉ NA ASSIGNMENT
// ========================================
model AssignmentDocument {
  id               Int @id @default(autoincrement())
  userAssignmentId Int
  documentId       Int

  userAssignment UserAssignment @relation(fields: [userAssignmentId], references: [id], onDelete: Cascade)
  document       Document       @relation(fields: [documentId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())
}

// Single-use, expirable opaque reset tokens
model PasswordResetToken {
  id        Int      @id @default(autoincrement())
  token     String   @unique
  email     String
  userId    Int
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  createdAt DateTime @default(now())
  used      Boolean  @default(false)
  usedAt    DateTime?

  @@index([email])
  @@index([userId])
}
